spring.application.name=car-bike-rental

# =====================================================================
# MONGODB ATLAS (Cloud Database)
# =====================================================================
# 1. Go to https://cloud.mongodb.com and create a free cluster.
# 2. Click: Database -> Connect -> Drivers -> Java.
# 3. Copy your connection string (looks like the one below).
# 4. Paste it into the line below AFTER '=' (replace the placeholders).
#    Example format:
#    spring.mongodb.uri=mongodb+srv://YOUR_USERNAME:YOUR_PASSWORD@cluster0.abCDefG.mongodb.net/rental_db?retryWrites=true&w=majority&appName=Cluster0
# 5. In Atlas, add your current IP to the Network Access allow list
#    (or set it to 0.0.0.0/0 for "Anywhere" while developing).
# 6. Make sure your Database User has read/write access.
#
# Uncomment the two lines below and fill in YOUR Atlas connection string:
spring.mongodb.uri=mongodb+srv://saisugnan25_db_user:VsRVKoz1javuviCJ@carrental.fwse2mw.mongodb.net/?appName=CarRental
#
# If the URI above is left commented out, the app connects to MongoDB
# running locally on your machine (default port 27017) using this database name:
spring.mongodb.database=rental_db

# File Upload
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Server
server.port=8082